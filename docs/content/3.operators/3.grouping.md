---
title: Grouping
description: Parentheses. Learn how to group expressions in pb-query.
navigation:
  icon: i-lucide-parentheses
---

### `.group(callback)`

Creates a logical group.

```ts
pbQuery<Post>().group((q) => q.equal('status', 'active').or().equal('status', 'inactive')); // (status~'active' || status~'inactive')
```
